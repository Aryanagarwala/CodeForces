#include <iostream>
#include <cmath>
#include <vector>
using namespace std;
int32_t main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int n;
    cin>>n;
    string str;
    cin>>str;
    cout<<(n+1)<<endl;
    return 0;
}